const navbarToggleBtn = document.querySelector('.navbar-toggle-btn').addEventListener('click', function () {
  const navbarMenu = document.querySelector('.navbar-menu').classList.toggle('hidden');
});
